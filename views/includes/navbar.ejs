<nav class="navbar navbar-expand-lg border-bottom sticky-top custom-navbar">
  <div class="container-fluid px-lg-4 px-3">

    <!-- LEFT -->
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <i class="fa-regular fa-compass"></i>
      <span class="brand-text ms-2">Home</span>
    </a>

    <!-- MOBILE TOGGLER -->
    <button
      class="navbar-toggler border-0 shadow-none"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <i class="fa-solid fa-bars"></i>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- CENTER SEARCH -->
      <div class="mx-auto search-wrapper">
        <form action="/listings" method="GET" class="search-form">

          <input
            class="form-control search-inp"
            type="search"
            name="search"
            placeholder="Search destinations"
            value="<%= search || '' %>"
          />

          <button class="btn-search" type="submit">
            <span class="search-text">Search</span>
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
          </button>

        </form>
      </div>

      <!-- RIGHT LINKS -->
      <div class="navbar-nav ms-auto nav-right">

        <a class="nav-link fw-semibold" href="/listings/new">
          Add New Listing
        </a>

        <% if(!currUser) { %>

          <a class="nav-link fw-semibold" href="/signup">
            Sign up
          </a>

          <a class="nav-link fw-semibold" href="/login">
            Log in
          </a>

        <% } %>

        <% if(currUser) { %>

          <a class="nav-link fw-semibold" href="/logout">
            Log out
          </a>

        <% } %>

      </div>
    </div>
  </div>
</nav>